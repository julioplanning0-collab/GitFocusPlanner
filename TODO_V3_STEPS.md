# TODO V3 - 25 Étapes du Flux Complet

**Source**: FLUX_V3_CORRECT.md
**Branch**: v3-linear-timeline-pure
**Consigne**: Cocher APRÈS implémentation ET tests de chaque étape

---

## PHASE 1: Chargement Page (Frontend Init)

- [ ] **Étape 1**: Init Planning Start Time
  - Calcul: now + 15min, arrondi au multiple de 5 supérieur
  - `state.planningStartTime = Date object`

- [ ] **Étape 2**: Load Tasks from API (3 calls parallel)
  - GET `/api/v3/tasks/pomodoro` → `state.pomodoroTasks`
  - GET `/api/v3/tasks/recurrent?pause_only=true` → `state.pauseTasks`
  - GET `/api/v3/tasks/recurrent?exclude_pauses=true` → `state.recurrentTasks`

- [ ] **Étape 3**: Load Obstacles
  - GET `/api/v3/temps-morts?date=YYYY-MM-DD` → `state.tempsMorts`

---

## PHASE 2: Sélection Utilisateur (Frontend)

- [ ] **Étape 4**: User Selects Pomodoros
  - Checkboxes onglet Pomodoro
  - `state.selectedPomodoroIds = ["TASK001", ...]`

- [ ] **Étape 5**: User Drags Pauses (✅ NOUVEAU)
  - Drag & drop onglet Pauses
  - Ordre préservé, duplicatas autorisés
  - `state.selectedPauseIds = ["REC001", "REC003", "REC001"]`

- [ ] **Étape 6**: User Toggles Récurrentes
  - Toggle switches ON/OFF
  - `state.enabledRecurrentIds = ["REC002", ...]`

- [ ] **Étape 7**: User Config Options
  - `calin_enabled`, `clope_enabled`, `clope_interval_min`

---

## PHASE 3: Génération Backend (ORDERING)

- [ ] **Étape 8**: Frontend → Backend Request
  - POST `/api/v3/planning/generate`
  - Body: `{date, planning_start_time, pomodoro_ids, pause_ids, recurrent_ids, options}`

- [ ] **Étape 9**: Backend Load Data
  - Load selected Pomodoros by IDs
  - Load selected Pauses by IDs (preserve order & duplicates!)
  - Load active Recurrents
  - Load Planned tasks for date
  - Load Temps morts for date

- [ ] **Étape 10**: Expand Pomodoros
  - Task 75min → 3x Pomodoro 25min
  - `pomodoroIndex`, `pomodoroTotal`
  - `minuteOffset = 0` (placeholder)

- [ ] **Étape 11**: Alternate Pomodoros & Pauses (✅ LOGIQUE CLÉ)
  - Cycle through pauses (with duplicates preserved)
  - `[P1, Pause1, P2, Pause2, P3, Pause1 (dup), ...]`

- [ ] **Étape 12**: Insert Calins (Optional)
  - Every 2 pauses, insert CALIN (5min)
  - `autoGenerated: true`

- [ ] **Étape 13**: Insert Clopes (Optional)
  - Cumulative work >= interval_min → insert CLOPE (25min)
  - Reset counter after insertion

**Résultat Step 1**: `ordered_tasks` (liste sans heures calculées)

---

## PHASE 4: Génération Backend (TIME CALCULATION)

- [ ] **Étape 14**: Convert Temps Morts to Linear Obstacles
  - CSV `temps_morts` → `Obstacle[]` with `startMinute`, `endMinute`
  - Skip past obstacles (endMinute < 0)

- [ ] **Étape 15**: Rebuild Timeline (✅ FONCTION CENTRALE)
  - `rebuildTimeline(ordered_tasks, obstacles)`
  - Calculate `minuteOffset` for ALL tasks sequentially
  - Start at minute 0, advance by duration

- [ ] **Étape 16**: Find Next Free Slot (Collision Detection)
  - `findNextFreeSlot(start, duration, obstacles)`
  - Jump past obstacles if collision
  - Return first free slot

- [ ] **Étape 17**: Insert Planned Tasks
  - Place at requested time if possible
  - Find closest free slot if collision
  - Mark `rescheduled: true` if moved
  - Re-sort timeline by `minuteOffset`

**Résultat Step 2**: `timeline` (avec `minuteOffset` calculés)

---

## PHASE 5: Backend - Conversion Display

- [ ] **Étape 18**: Convert to Absolute Times
  - `minuteOffset` → `{date, heure_debut, heure_fin}`
  - `minutesToDateTime(minutes, planningStart)`
  - Format: `YYYY-MM-DD`, `HH:MM`

- [ ] **Étape 19**: Calculate Statistics
  - `total_pomodoros`, `total_pauses`, `total_work_min`, `total_pause_min`
  - `planning_start`, `planning_end`

- [ ] **Étape 20**: Return JSON Response
  - `{success: true, date, planning: display_slots, statistics}`

---

## PHASE 6: Frontend Display

- [ ] **Étape 21**: Receive Planning JSON
  - `state.currentPlanning = data.planning`
  - Save for export later

- [ ] **Étape 22**: Render Planning Timeline
  - Loop through `planning` array
  - Create HTML elements with colors by type
  - Display pomodoro badges (1/3, 2/3)
  - Mark rescheduled tasks

---

## PHASE 7: Export CSV

- [ ] **Étape 23**: User Clicks Export Button
  - POST `/api/v3/planning/export`
  - Body: `{date, planning: state.currentPlanning}`

- [ ] **Étape 24**: Backend Export to CSV
  - `write_planning_csv(csv_path, planning)` (ATOMIC!)
  - Temp file + rename pattern

- [ ] **Étape 25**: Log Pause Usage (Machine Learning)
  - Append to `pause_usage_history.csv`
  - For future intelligent scoring

---

## ✅ VALIDATION GLOBALE

- [ ] Toutes les 25 étapes cochées
- [ ] Tests unitaires V3 passent
- [ ] Tests guards passent (`test_guards.py`)
- [ ] Interface fonctionne end-to-end
- [ ] Export CSV produit fichier valide
- [ ] GUARD_V3.md checklist respectée
- [ ] Aucun import depuis `backend/planning_engine/` (ancien code)
- [ ] Architecture 2-step respectée (ORDERING → TIME CALCULATION)

---

**COCHER UNIQUEMENT APRÈS IMPLÉMENTATION + TESTS RÉUSSIS**

Date: 2025-11-06
Version: V3 Pure - Timeline Linéaire
Branch: v3-linear-timeline-pure
